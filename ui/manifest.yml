---
applications:
  - name: sbw2csv
    memory: 256M
    buildpacks:
      - https://github.com/cloudfoundry/nginx-buildpack

    # node Exress services deployed as sidecar
    sidecars:
      - name: services
        process_types: ['web']
        command: yarn start
